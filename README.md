# my-ai-project

一个基于 OpenAI API 的 Python AI 项目。

## 功能特点
- 支持 OpenAI GPT 模型调用
- 使用 `python-dotenv` 管理环境变量
- 支持数据处理（pandas）
- 适合快速原型开发和部署到 Railway / Vercel 等平台
- 实时查询任意 OKX 永续合约行情，（如输入：查询BTC）即自动生成趋势判断、关键支撑阻力位、目标价位及交易建议。

## 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
