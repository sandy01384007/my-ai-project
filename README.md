# my-ai-project

一个基于 OpenAI API 的 Python AI 项目。

## 功能特点
- 支持 OpenAI GPT 模型调用
- 使用 `python-dotenv` 管理环境变量
- 支持数据处理（pandas）
- 适合快速原型开发和部署到 Railway / Vercel 等平台

## 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
